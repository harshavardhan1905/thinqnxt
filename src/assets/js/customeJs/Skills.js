// console.log('JS LOADED');

// const section = document.querySelector('.skill-track-section');
// if (!section) return;

// section.addEventListener('click', (e) => {
//   const tab = e.target.closest('.tool-tab');
//   if (!tab) return;

//   const tabs = section.querySelectorAll('.tool-tab');
//   const groups = section.querySelectorAll('.tool-group');

//   tabs.forEach(t => t.classList.remove('is-active'));
//   groups.forEach(g => g.classList.remove('is-active'));

//   tab.classList.add('is-active');
//   document.getElementById(tab.dataset.target).classList.add('is-active');
// });
